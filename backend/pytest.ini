[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers
markers =
    integration: requires the PostGIS and Redis-backed API to be running
    e2e: requires the complete Docker Compose stack through the gateway
